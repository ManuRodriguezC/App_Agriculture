import 'package:flutter/material.dart';

// Principal color green
Color greenColor = Color.fromARGB(0, 70, 216, 70);

// Format of the principals titles in the app
TextStyle loginTitle = const TextStyle(
    fontFamily: 'Avenir',
    fontWeight: FontWeight.bold,
    color: Colors.green,
    fontSize: 50);

// Format for all usernames of the users register
TextStyle userName = const TextStyle(
    fontFamily: 'Avenir',
    fontWeight: FontWeight.bold,
    color: Colors.blue,
    fontSize: 20);

// Format of the all information of all users in the users voew page
TextStyle userInformation = const TextStyle(
    fontFamily: 'Avenir',
    fontWeight: FontWeight.w400,
    color: Colors.black,
    fontSize: 15);
